var documenterSearchIndex = {"docs":
[{"category":"section","location":"predictive/#Predictive-utilities-and-log-density-accessors","page":"Predictive utilities","text":"This page documents src/predict.jl — prior-predictive sampling, posterior predictive sampling, and point-wise log-density evaluation, all built on top of PracticalBayes.PriorMode and PracticalBayes.FixedMode (see their own docstrings for the exact per-site semantics; this file is the convenience layer on top).","title":"Predictive utilities and log-density accessors"},{"category":"section","location":"predictive/#Prior-and-prior-predictive-sampling","page":"Predictive utilities","text":"Every assumed site is drawn fresh from its prior; every observe site uses real data if the model has any bound (via arguments or conditioning), otherwise is drawn from the likelihood too. The return value is a NamedTuple covering every site — assumed and observed — since a common use is generating a synthetic (params..., data...) dataset for the model itself, not just the parameters.\n\nusing PracticalBayes\nusing Distributions\nusing Random\n\nPracticalBayes.@model function toy(n)\n    μ ~ Normal(0, 1)\n    σ ~ Exponential(1)\n    y ~ PracticalBayes.arraydist(Normal.(fill(μ, n), σ))\nend\n\nm = toy(5)\nrand(Random.Xoshiro(1), m)","title":"Prior and prior-predictive sampling"},{"category":"section","location":"predictive/#Log-density-at-a-fixed-point","page":"Predictive utilities","text":"logjoint/logprior/loglikelihood_at all evaluate the model at a fixed NamedTuple of parameter values (e.g. one posterior draw). Unlike predict below, every observe site must resolve to real data — there is no \"sample instead\" fallback, since you can't score a log-density against a missing observation.","title":"Log-density at a fixed point"},{"category":"section","location":"predictive/#returned:-read-back-the-model's-own-return-value","page":"Predictive utilities","text":"Useful for models whose body computes and returns a derived quantity (a transformed parameter, a summary statistic) that you want evaluated at a specific posterior draw rather than re-derived by hand outside the model.","title":"returned: read back the model's own return value"},{"category":"section","location":"predictive/#Posterior-predictive-sampling","page":"Predictive utilities","text":"predict is the one place un-conditioned observe sites are sampled (unlike logjoint/returned) — that's the whole point of a posterior predictive check. chain_draws turns a SymChain (as returned by AbstractMCMC.sample, see Sampling) back into the Vector{NamedTuple} form predict expects, so the usual flow is:\n\nusing PracticalBayes\nusing Distributions\nusing AdvancedHMC\nusing Random\nimport AbstractMCMC\n\nPracticalBayes.@model function reg(x, y)\n    β ~ Normal(0, 1)\n    σ ~ Exponential(1)\n    y ~ PracticalBayes.arraydist(Normal.(β .* x, σ))\nend\n\nx = randn(Random.Xoshiro(1), 20)\ny = 2.0 .* x .+ randn(Random.Xoshiro(2), 20) .* 0.3\nm = reg(x, y)\n\nchn = AbstractMCMC.sample(Random.Xoshiro(3), m, NUTS(0.8), 200;\n    n_adapts=100, discard_initial=100, progress=false)\ndraws = PracticalBayes.chain_draws(chn)\npreds = PracticalBayes.predict(Random.Xoshiro(4), m, draws[1:10])\nlength(preds)","title":"Posterior predictive sampling"},{"category":"section","location":"predictive/#Pointwise-log-likelihoods-(LOO-CV/WAIC)","page":"Predictive utilities","text":"loglikelihood_at above gives one summed scalar; LOO-CV/WAIC-style model comparison (e.g. via ParetoSmooth.jl or exporting to ArviZ) instead needs one log-likelihood value per observation. This is a separate, opt-in re-evaluation of the model — never run during sampling — because computing per-observation values requires logpdf.(dist, y) at every .~ site, which is deliberately NOT what the hot HMC path computes (see PracticalBayes.PointwiseMode's own docstring for why the fast summed .~ path can't just be reused here).\n\nusing PracticalBayes\nusing Distributions\nusing Random\n\nPracticalBayes.@model function reg2(x, y)\n    β ~ Normal(0, 1)\n    σ ~ Exponential(1)\n    y .~ Normal.(β .* x, σ)\nend\n\nx = randn(Random.Xoshiro(1), 20)\ny = 2.0 .* x .+ randn(Random.Xoshiro(2), 20) .* 0.3\nm = reg2(x, y)\n\npw = PracticalBayes.pointwise_loglikelihoods(m, (β=1.9, σ=0.35))\nsum(pw.y) ≈ PracticalBayes.loglikelihood_at(m, (β=1.9, σ=0.35))","title":"Pointwise log-likelihoods (LOO-CV/WAIC)"},{"category":"method","location":"predictive/#Base.rand-Tuple{AbstractRNG, Model}","page":"Predictive utilities","text":"rand([rng,] model::Model) -> NamedTuple\nrand([rng,] model::Model, n::Integer) -> Vector{NamedTuple}\n\nDraw one (or n independent) sample(s) from model's prior/prior-predictive distribution: every assumed site is drawn fresh from its prior; every observe site uses real data if model has any bound (via arguments or conditioning), otherwise is drawn from the likelihood. Returns a NamedTuple mapping every site name (assumed AND observed) to its value — this is PriorMode's own values accumulator (see modes.jl), not filtered down to just the parameters, since a common use is generating synthetic (params..., data...) datasets for the model itself.\n\n\n\n\n\n","title":"Base.rand"},{"category":"method","location":"predictive/#PracticalBayes.logjoint-Tuple{Model, NamedTuple}","page":"Predictive utilities","text":"logjoint(model::Model, nt::NamedTuple) -> Real\nlogprior(model::Model, nt::NamedTuple) -> Real\nloglikelihood(model::Model, nt::NamedTuple) -> Real\n\nEvaluate the model's log-joint/log-prior/log-likelihood at a fixed point nt (a NamedTuple of parameter values, e.g. one posterior draw). Every observe site must resolve to real data (via model's own arguments or conditioning) — there is no predict=true escape hatch here, since a log-density with a missing observation term isn't a well-defined number.\n\nThese share their names with the Accum-level accessors in accumulator.jl (logjoint(acc::Accum), etc.) — Julia's multiple dispatch picks the right method from the argument types, so both are always available un-qualified.\n\n\n\n\n\n","title":"PracticalBayes.logjoint"},{"category":"function","location":"predictive/#PracticalBayes.returned","page":"Predictive utilities","text":"returned(model::Model, nt::NamedTuple; rng=Random.default_rng())\n\nRuns model with every assumed site fixed to nt's values (same FixedMode/predict=false semantics as logjoint) and returns the model function's own return value — whatever the @model body's last expression evaluates to (a :=-computed summary statistic, e.g.), not the log-density.\n\n\n\n\n\n","title":"PracticalBayes.returned"},{"category":"function","location":"predictive/#PracticalBayes.predict","page":"Predictive utilities","text":"predict([rng,] model::Model, draws) -> Vector{NamedTuple}\n\nPosterior-predictive sampling: for each parameter draw in draws (any iterable of NamedTuples, e.g. the output of rand(model, n), or built by hand from a FlexiChains.SymChain — see below), fixes the model's assumed sites to that draw's values and samples every un-conditioned observe site fresh from the likelihood (FixedMode(...; predict=true)). model is typically NOT the same model instance used for inference — pass one with the observed argument(s) replaced by an array of missing at the desired output shape (the standard convention; see .~'s predictive-sampling support in tilde.jl for why the array must carry shape, not a bare missing), e.g.:\n\nm_train = regression(x_train, y_train)\n# ... run `sample` on m_train, get a `chn::SymChain` ...\nm_test = regression(x_test, fill(missing, length(x_test)))\ndraws = chain_draws(chn)  # see below\npreds = predict(m_test, draws)\n\nReturns a Vector{NamedTuple}, one per input draw, containing every site's value (fixed parameters AND freshly-sampled observations) — same shape as rand's output.\n\n\n\n\n\n","title":"PracticalBayes.predict"},{"category":"function","location":"predictive/#PracticalBayes.chain_draws","page":"Predictive utilities","text":"chain_draws(chn::FlexiChains.SymChain) -> Vector{NamedTuple}\n\nFlattens every (iteration, chain) draw in chn into one Vector{NamedTuple} (all chains pooled together, in chain-major then iteration order), suitable as the draws argument to predict/returned/logjoint etc. Only Parameter keys are included (AdvancedHMC's diagnostic Extra keys, e.g. acceptance_rate, are dropped) — a model's FixedMode only ever reads fields it recognizes by name, so including the extras would be harmless but pointless; this keeps each NamedTuple exactly the shape a model expects.\n\n\n\n\n\n","title":"PracticalBayes.chain_draws"},{"category":"function","location":"predictive/#PracticalBayes.pointwise_loglikelihoods","page":"Predictive utilities","text":"pointwise_loglikelihoods(model::Model, nt::NamedTuple; flatten=false)\n    -> NamedTuple or Vector{Float64}\n\nPer-observation log-likelihood at a fixed point nt (e.g. one posterior draw) — the building block for LOO-CV/WAIC-style model comparison (e.g. via ParetoSmooth.jl or exporting to ArviZ), which need one log-likelihood value per observation rather than loglikelihood_at's single summed scalar. Same \"every observe site must resolve to real data\" contract as logjoint/loglikelihood_at — there is no predict=true form of this, since a per-observation likelihood term is meaningless without a real observation to score it against.\n\nBy default (flatten=false) returns a NamedTuple mapping each observe site's name to its own Vector of per-observation values, preserving site identity for models with more than one observe block. Pass flatten=true to get everything concatenated into a single Vector{Float64} (in the order sites were visited during evaluation), which is the shape most LOO-CV/WAIC tooling expects directly.\n\nThis is a genuinely separate, opt-in re-evaluation of the model — it does NOT reuse any state from sampling — because computing per-observation values requires logpdf.(dist, y) at every .~ site, which is deliberately NOT what the hot HMC path computes (see PointwiseMode's own docstring for why). Expect to call this once per posterior draw you want pointwise values for, not inside any performance-sensitive loop.\n\ndraws = chain_draws(chn)\npw = [pointwise_loglikelihoods(model, nt) for nt in draws]  # one NamedTuple per draw\nloo_matrix = reduce(hcat, pointwise_loglikelihoods(model, nt; flatten=true) for nt in draws)  # n_obs × n_draws\n\n\n\n\n\n","title":"PracticalBayes.pointwise_loglikelihoods"},{"category":"section","location":"sampling/#Sampling-and-chains","page":"Sampling","text":"This page documents the sampling and chain-building machinery introduced around src/sample.jl, src/gibbs.jl, and src/latent.jl.","title":"Sampling and chains"},{"category":"section","location":"sampling/#One-shot-HMC/NUTS-sampling","page":"Sampling","text":"PracticalBayes extends AbstractMCMC.sample for Model + AdvancedHMC samplers.","title":"One-shot HMC/NUTS sampling"},{"category":"section","location":"sampling/#Behavior","page":"Sampling","text":"Builds Layout, θ0, and LogDensityFunction from the model.\nRuns AdvancedHMC through AbstractMCMC.LogDensityModel.\nBy default, returns a FlexiChains.SymChain with constrained parameter fields and sampler diagnostics.\nUse chain_type=nothing to receive raw Vector{AdvancedHMC.Transition}.","title":"Behavior"},{"category":"section","location":"sampling/#Gibbs-blocks-for-mixed-latent/continuous-models","page":"Sampling","text":"","title":"Gibbs blocks for mixed latent/continuous models"},{"category":"section","location":"sampling/#Block-design","page":"Sampling","text":"Gibbs composes blocks of the form:\n\n:name => NUTS(0.8) for continuous/HMC-updated variables\n:name => MyLatentKernel() for discrete or custom latent updates\n\nEvery assumed variable must belong to exactly one block.","title":"Block design"},{"category":"section","location":"sampling/#Latent-kernel-contract","page":"Sampling","text":"To implement a custom latent update:\n\nDefine a subtype of AbstractLatentKernel.\nImplement:\n\nPracticalBayes.latent_step(rng, kernel::MyKernel, block_names, c::ModelConditional)\n\nReturn a NamedTuple with exactly the keys in block_names.\n\nThe latent step runs once per Gibbs sweep and is never called from inside gradient/leapfrog internals.","title":"Latent kernel contract"},{"category":"section","location":"sampling/#Example:-Gibbs-with-a-custom-latent-kernel","page":"Sampling","text":"using PracticalBayes\nusing Distributions\nusing AdvancedHMC\nusing Random\nimport AbstractMCMC\n\nPracticalBayes.@model function toy(y)\n    μ ~ Normal(0, 1)\n    z ~ Normal(μ, 1)\n    y ~ Normal(z, 0.5)\nend\n\nstruct SimpleLatent <: PracticalBayes.AbstractLatentKernel end\n\nfunction PracticalBayes.latent_step(rng, ::SimpleLatent, block_names, c::PracticalBayes.ModelConditional)\n    block_names == (:z,) || error(\"SimpleLatent handles only :z\")\n    return (; z = rand(rng, Normal(c.values.μ, 1.0)))\nend\n\nm = toy(2.0)\nspl = PracticalBayes.Gibbs(:μ => NUTS(0.8), :z => SimpleLatent())\nrng = Random.Xoshiro(1)\ntransition, state = AbstractMCMC.step(rng, m, spl)\ntransition","title":"Example: Gibbs with a custom latent kernel"},{"category":"section","location":"sampling/#Notes-for-large-models","page":"Sampling","text":"Use build_layout(...; T=Float32) and Float32 distribution literals (1f0, 0f0) to keep parameter/AD paths in Float32.\nUse untracked parameters where appropriate to avoid unnecessary optimization/sampling dimensions.\nFor long Gibbs runs, keep n_adapts fixed from the first sweep onward for HMC blocks.","title":"Notes for large models"},{"category":"function","location":"sampling/#StatsBase.sample","page":"Sampling","text":"sample(rng, model::Model, spl::AdvancedHMC.AbstractHMCSampler, N;\n       adtype=AutoForwardDiff(), init=NamedTuple(), chain_type=SymChain,\n       kwargs...)\n\nBuild a Layout + LogDensityFunction for model and run spl (e.g. AdvancedHMC.NUTS(0.8)) for N iterations via AbstractMCMC.sample on the resulting LogDensityModel. Returns a FlexiChains.SymChain (FlexiChain{Symbol}) by default — every constrained parameter name in model becomes a Parameter(:name) key (invlinked back from the sampler's raw unconstrained draws via Layout), and every AdvancedHMC per-iteration statistic (log_density, acceptance_rate, n_steps, ...) becomes an Extra(:stat_name) key alongside it — so chn[:mu], chn[Extra(:n_steps)], summarystats(chn), etc. all work exactly as they would on a chain from a plain NamedTuple-transition AbstractMCMC sampler.\n\nNOTE on how this actually works: FlexiChains.to_nt_and_stats(nt::NamedTuple) puts the ENTIRE NamedTuple into the params half and returns EMPTY stats — confirmed directly (a first attempt that merged params and stats into one flat NamedTuple before calling bundle_samples silently produced a SymChain where every AdvancedHMC diagnostic showed up as a Parameter, not an Extra). The params/stats split has to survive as two SEPARATE fields for FlexiChains to route them correctly, so _pb_transition_to_nt returns a small PBTransitionNT wrapper (below) with its own to_nt_and_stats method, rather than a bare NamedTuple.\n\nchain_type=nothing returns AdvancedHMC's raw Vector{AdvancedHMC.Transition} instead (skips invlink/FlexiChains entirely) for callers who want the unconstrained θ vectors directly (e.g. diagnostics, predict).\n\nMulti-chain: AbstractMCMC.MCMCThreads/MCMCDistributed work with NO\n\nPracticalBayes-side code beyond this single-chain method\n\nAbstractMCMC.sample(rng, model, spl, MCMCThreads(), N, nchains; kwargs...) just works — confirmed directly, no extra method needed. AbstractMCMC's own mcmcsample implementation for MCMCThreads calls this exact single-chain method once per thread (via StatsBase.sample, deepcopyd model/sampler per thread) and combines the resulting SymChains with chainsstack/chainscat (reduce(chainscat, chains), i.e. cat(chains...; dims=3)) — FlexiChain (backed by DimensionalData) supports this cat generically, with no FlexiChains-side or PracticalBayes-side multi-chain glue required. Verified: 4 threaded NUTS chains on a conjugate model combine into one correctly-shaped (niters, nchains) SymChain, and FlexiChains.rhat on the result reports < 1.01 for both parameters (the M2 milestone's own multi-chain gate).\n\n\n\n\n\n","title":"StatsBase.sample"},{"category":"type","location":"sampling/#PracticalBayes.Gibbs","page":"Sampling","text":"Gibbs(pairs::Pair...)\n\nA block-wise Gibbs sampler: Gibbs(:theta => NUTS(0.8), :z => MyKernel()). Each Pair's left side is a Symbol or Tuple{Vararg{Symbol}} naming the block's model variables; the right side is either an AbstractHMCSampler (NUTS/HMC/HMCDA from AdvancedHMC) or a user's AbstractLatentKernel subtype. Every assumed variable in the model must appear in EXACTLY one block — checked (with a clear error naming any missing/duplicated name) the first time AbstractMCMC.step runs against a concrete model, since Gibbs itself is constructed without a model reference (matching how a plain NUTS(0.8) is also model-agnostic until it's actually used to sample).\n\n\n\n\n\n","title":"PracticalBayes.Gibbs"},{"category":"type","location":"sampling/#PracticalBayes.GibbsState","page":"Sampling","text":"GibbsState(values, subs)\n\nvalues: the current constrained-space value of EVERY assumed model variable (all blocks merged into one NamedTuple) — this is also what a Gibbs sweep returns as its transition. subs: a Tuple, same length/order as the owning Gibbs's blocks, of each block's persistent GibbsSamplerSub/GibbsLatentSub.\n\n\n\n\n\n","title":"PracticalBayes.GibbsState"},{"category":"type","location":"sampling/#PracticalBayes.AbstractLatentKernel","page":"Sampling","text":"AbstractLatentKernel\n\nSupertype for user-defined Gibbs latent-block samplers. A subtype K must implement latent_step(rng, kernel::K, block_names, c::ModelConditional) (see its docstring). Gibbs (gibbs.jl) never differentiates through a latent kernel and calls it exactly once per outer sweep — never from inside a NUTS leapfrog/tree-doubling step — so a kernel is free to do arbitrary, non-differentiable work (discrete sampling, matrix factorizations, calling back into rand, etc).\n\nIf a kernel's own struct carries MUTABLE internal state (e.g. an adaptive proposal's running acceptance count), make sure that state is safe to deepcopy — a future MCMCThreads-style multi-chain runner would isolate chains by deep-copying the sampler (matching how AbstractMCMC.MCMCThreads already isolates per-chain DI preps/tapes for the NUTS side).\n\n\n\n\n\n","title":"PracticalBayes.AbstractLatentKernel"},{"category":"type","location":"sampling/#PracticalBayes.ModelConditional","page":"Sampling","text":"ModelConditional(model, values)\n\nPassed to latent_step. values is a NamedTuple holding every OTHER Gibbs block's current (constrained-space) value — i.e. everything needed to evaluate this block's conditional distribution given the rest of the model. Data isn't duplicated into values: read it directly off c.model.args (the model's own call arguments) or c.model.conditioned, exactly as you would from inside the @model body itself.\n\n\n\n\n\n","title":"PracticalBayes.ModelConditional"},{"category":"function","location":"sampling/#PracticalBayes.latent_step","page":"Sampling","text":"latent_step(rng, kernel::AbstractLatentKernel, block_names::Tuple{Vararg{Symbol}},\n            c::ModelConditional) -> NamedTuple\n\nUser-implemented. Must return a NamedTuple with EXACTLY the keys in block_names, holding the new constrained-space draw for each. Called ONCE per Gibbs sweep by Gibbs's sampling loop (gibbs.jl) — never inside a gradient call, never mid-leapfrog-step — so latent_step is free to use plain Float64/Int arithmetic throughout; nothing here is ever differentiated or promoted to a Dual.\n\nExample: a hand-written forward-filter/backward-sample (FFBS) kernel for a\n\n2-state Gaussian HMM's discrete state sequence z\n\nusing Distributions, StatsFuns, PracticalBayes\n\n@model function hmm(y)\n    p_stay ~ Beta(8, 2)\n    sigma ~ Exponential(1)\n    N = length(y)\n    z = Vector{Int}(undef, N)   # discrete latent container — plain Int is fine,\n                                # NEVER touched by AD (see `paramtype` docstring:\n                                # only CONTINUOUS assumed containers need it)\n    z[1] ~ Categorical([0.5, 0.5])\n    for t in 2:N\n        z[t] ~ Categorical(z[t - 1] == 1 ? [p_stay, 1 - p_stay] : [1 - p_stay, p_stay])\n    end\n    for t in 1:N\n        y[t] ~ Normal(z[t] == 1 ? 0.0 : 5.0, sigma)\n    end\nend\n\nstruct FFBS <: AbstractLatentKernel end\n\nfunction PracticalBayes.latent_step(rng, ::FFBS, block_names, c::ModelConditional)\n    block_names == (:z,) || error(\"this FFBS kernel only handles the `:z` block\")\n    y = c.model.args.y\n    p_stay, sigma = c.values.p_stay, c.values.sigma\n    N = length(y)\n    P = [p_stay 1-p_stay; 1-p_stay p_stay]\n    means = (0.0, 5.0)\n\n    # forward filter\n    logα = Matrix{Float64}(undef, 2, N)\n    logα[:, 1] .= log(0.5) .+ logpdf.(Normal.(means, sigma), y[1])\n    for t in 2:N, j in 1:2\n        logα[j, t] = logsumexp(logα[:, t - 1] .+ log.(P[:, j])) + logpdf(Normal(means[j], sigma), y[t])\n    end\n\n    # backward sample\n    z = Vector{Int}(undef, N)\n    z[N] = rand(rng, Categorical(softmax(logα[:, N])))\n    for t in (N - 1):-1:1\n        w = softmax(logα[:, t] .+ log.(P[:, z[t + 1]]))\n        z[t] = rand(rng, Categorical(w))\n    end\n    return (; z=z)\nend\n\nchain = ...  # Gibbs(:p_stay => NUTS(0.8), :sigma => NUTS(0.8), :z => FFBS())\n\n\n\n\n\n","title":"PracticalBayes.latent_step"},{"category":"section","location":"float32_gpu/#Float32-and-GPU-usage","page":"Float32 and GPU","text":"PracticalBayes is Float32-first: the parameter vector θ can be Float32 end-to-end (layout, log-density, gradient, HMC state), which roughly halves memory traffic on large models and is a prerequisite for GPU data. This page covers the two together since GPU support builds directly on the Float32 path — a model that isn't correctly Float32-safe won't be GPU-safe either.","title":"Float32 and GPU usage"},{"category":"section","location":"float32_gpu/#Float32-parameters","page":"Float32 and GPU","text":"build_layout(model; T=Float32) makes the parameter vector θ0 Float32, but that alone is not enough to get a genuinely end-to-end Float32 computation — silent promotion back to Float64 is the main pitfall:\n\nDistribution literals need f0 suffixes. Exponential(1.0f0), not Exponential(1) — a bare 1 is an Int/Float64 literal that promotes any Float32 computation touching it back to Float64.\nVector-valued prior means need paramtype, not zeros(k). zeros(paramtype(__mode__), k), not bare zeros(k) (which defaults to Float64) — see PracticalBayes.paramtype for how the mode tracks the \"current\" element type. This is easy to get wrong in a way that still runs and still produces correct numbers, just silently back in Float64.\nData (X/y) can stay Float64 without breaking Float32 propagation through the gradient or costing performance — confirmed directly. Only parameters and distribution literals need the f0/paramtype treatment.\nAdvancedHMC.NUTS(δ) fixes its step-size type to typeof(δ). Passing a bare 0.8 (Float64) together with a Float32 θ0 fails during step-size adaptation — use NUTS(eltype(θ0)(0.8)) or, equivalently, NUTS(0.8f0).\n\nusing PracticalBayes\nusing Distributions\nusing ADTypes\nusing LogDensityProblems\n\nPracticalBayes.@model function f32_demo(y)\n    pT = PracticalBayes.paramtype(__mode__)\n    μ ~ Normal(zero(pT), one(pT))\n    σ ~ Exponential(one(pT))\n    y .~ Normal.(μ, σ)\nend\n\ny32 = Float32.(randn(20) .+ 2)\nm = f32_demo(y32)\nlayout, θ0, store0 = PracticalBayes.build_layout(m; T=Float32)\neltype(θ0)\n\nusing PracticalBayes\nusing Distributions\nusing ADTypes\nusing LogDensityProblems\n\nPracticalBayes.@model function f32_demo2(y)\n    pT = PracticalBayes.paramtype(__mode__)\n    μ ~ Normal(zero(pT), one(pT))\n    σ ~ Exponential(one(pT))\n    y .~ Normal.(μ, σ)\nend\n\nm = f32_demo2(Float32.(randn(20) .+ 2))\nlayout, θ0, store0 = PracticalBayes.build_layout(m; T=Float32)\nldf = PracticalBayes.LogDensityFunction(m, layout, store0, ADTypes.AutoForwardDiff(); θ0=θ0)\nval, grad = LogDensityProblems.logdensity_and_gradient(ldf, θ0)\neltype(grad)","title":"Float32 parameters"},{"category":"section","location":"float32_gpu/#GPU","page":"Float32 and GPU","text":"Scope: the parameter vector θ stays on the CPU; data (X, y) can live on the GPU (CUDA.CuArray). This is deliberately narrower than a fully-device θ (tracked as an open M6 item) — it targets the common case where the model's per-observation work (a matrix-multiply, a vectorized observe) is the expensive part, not the parameter count.\n\nThe framework guarantee: no framework-introduced scalar indexing. Parameter reads in the hot path are view(θ, range), never θ[i] one element at a time, and the compiler never indexes into model data on your behalf. This means CUDA.allowscalar(false) — CUDA.jl's strict mode that throws on any scalar GPU-array indexing — is safe to enable, and doing so is the recommended way to catch accidental scalar indexing in your own model code early.\n\nWhat this means for model authors: use array-variate observes so the observe statement itself is a single vectorized GPU operation rather than a loop:\n\nPracticalBayes.@model function gpu_regression(X, y)\n    pT = PracticalBayes.paramtype(__mode__)\n    k = size(X, 2)\n    beta ~ Distributions.MvNormal(zeros(pT, k), LinearAlgebra.I)\n    sigma ~ Distributions.Exponential(one(pT))\n    eta = X * beta                                    # one GPU matmul\n    y ~ Distributions.MvNormal(eta, sigma^2 * LinearAlgebra.I)  # one vectorized observe\nend\n\nThen build X/y as CUDA.CuArrays before constructing the model — θ0 itself stays a plain CPU Vector:\n\nimport CUDA\nCUDA.allowscalar(false)\nX_gpu, y_gpu = CUDA.CuArray(X_cpu), CUDA.CuArray(y_cpu)\nm = gpu_regression(X_gpu, y_gpu)\nlayout, θ0, store0 = PracticalBayes.build_layout(m)  # θ0 is a CPU Vector\nldf = PracticalBayes.LogDensityFunction(m, layout, store0, ADTypes.AutoForwardDiff(); θ0=θ0)\nLogDensityProblems.logdensity_and_gradient(ldf, θ0)  # matmul/observe run on GPU, gradient assembled on CPU\n\nThis exact pattern is exercised by test/gpu/cuda.jl (gated on CUDA.functional()) and benchmarked (CPU vs GPU gradient time across an N sweep) by benchmarks/gpu/gpu_sweep.jl, meant to be run by hand on a machine with a real, working GPU — GitHub's standard hosted CI runners have none, so this isn't part of the always-on CI benchmark sweep.","title":"GPU"},{"category":"section","location":"#PracticalBayes.jl","page":"Home","text":"PracticalBayes is a performance-oriented probabilistic programming package with Turing-style model syntax and a strong focus on fast inference paths for large models.","title":"PracticalBayes.jl"},{"category":"section","location":"#Core-ideas","page":"Home","text":"Keep model authoring ergonomic (@model, ~, vectorized distributions).\nKeep AD and sampler internals explicit and composable.\nSupport large-model workloads with features like untracked nuisance parameters and Float32 parameter paths.\nWork with DifferentiationInterface-compatible backends, including ForwardDiff, Mooncake, and Enzyme.","title":"Core ideas"},{"category":"section","location":"#Getting-started","page":"Home","text":"using PracticalBayes\nusing ADTypes\nusing LogDensityProblems\nusing Distributions\n\nPracticalBayes.@model function demo(y)\n    μ ~ Normal(0, 1)\n    σ ~ Exponential(1)\n    y .~ Normal.(μ, σ)\nend\n\nm = demo(randn(20))\nlayout, θ0, store0 = PracticalBayes.build_layout(m)\nldf = PracticalBayes.LogDensityFunction(m, layout, store0, ADTypes.AutoForwardDiff(); θ0=θ0)\nLogDensityProblems.logdensity_and_gradient(ldf, θ0)\n\nSee Sampling for the new sampling/chain APIs (sample, Gibbs, AbstractLatentKernel, SymChain).","title":"Getting started"}]
}
